#--------------------------------------------------------------------------
#--------------------------------------------------------------------------
# minuet sector (name = local3)
#

#-----------------------------------------------
# Sector Name
name: local3

#-----------------------------------------------
# Sector Enable
enable: true

vhosts: 
  - name: public

    # Sector Listen Type 
    # Selected pattern http, https, webSocket, webSocketSSL
    type: http

    # Host Name (domain)
    host: localhost

    # Port Number
    port: 4851

    # SSL Setting (Type = https only)
    # ssl:
    #  key:  ssl/server.key
    #  cert: ssl/server.crt

  - name: admin

    # Sector Listen Type 
    # Selected pattern http, https, webSocket, webSocketSSL
    type: http

    # Host Name (domain)
    host: localhost

    # Port Number
    port: 7251

#-----------------------------------------------
# modules
modules:
  - cloud